/// Loading Text.
    
    // -- Update Text.
    loading_string = argument0;
    
    // -- Debug
    show_debug_message(argument0);
    
    // -- Freeze just to make sure
    application_freeze(50);
